matrix:
  include:
    - language: ruby
      sudo: false
      dist: trusty
      cache: bundler
      rvm:
        - 2.4.1
      services:
        - mongodb
      script:
        - bundle exec rake
        - bundle exec rubocop
    - language: node_js
      sudo: false
      dist: trusty
      node_js:
        - "node"
        - "lts/*"
      cache:
        directories:
          - "node_modules"
      script:
        - npm run lint
        - ./bin/validate_dist.sh
